apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-app.fullname" . }}
  labels:
    {{- include "apache-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.targetPort.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "apache-app.selectorLabels" . | nindent 4 }}
