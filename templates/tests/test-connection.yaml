apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apache-app.fullname" . }}-test-connection"
  labels:
    {{- include "apache-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apache-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
